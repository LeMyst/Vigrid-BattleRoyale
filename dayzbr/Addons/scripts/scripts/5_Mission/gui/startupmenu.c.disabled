modded class StartupMenu 
{
	//load br layout
	override Widget Init()
	{
		layoutRoot = GetGame().GetWorkspace().CreateWidgets("dayzbr/gui/layouts/br_startup.layout");
		
		m_label = TextWidget.Cast( layoutRoot.FindAnyWidget("TextWidget") );

		return layoutRoot;
	}
}