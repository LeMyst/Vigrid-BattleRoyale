<?xml version="1.0" encoding="UTF-8" standalone="yes" ?>
<!--
    One action, one key. No <exclude> group, and none is wanted: the toggle is a single press that
    changes nothing about what the player can do, so there is nothing to suppress. See
    Extra/Map/Data/Inputs.xml for the long version of why an exclude group is a trap when you DO
    think you want one.

    J, and it is free across this mod's other four Inputs.xml files - Battle Royale takes F1-F6 and
    the arrows, Party takes P/T/Y, Map takes M/N/K, AutoRun takes Z.

    NOT strictly unbound in vanilla, and the distinction is worth stating because only K, O and P
    genuinely are. kJ is UABuldSlow in P:\bin\preset_keymouseprimary.xml, but it sits under the
    BULDOZER SPECIFIC block - the Workbench model viewer, not the game - and its only reference in
    all of P:\scripts is the script console's own suppression list (scriptconsole.c:531). So it is
    unreachable in normal gameplay, which is exactly the standard AutoRun applied to kZ (UAMoveDown,
    under DEBUG SPECIFIC / Freefly Camera).

    Community-Online-Tools is the one to check if a bind ever does two things at once; its own
    Inputs.xml is what collided with Party's Y.
-->
<modded_inputs>
    <inputs>
        <actions>
            <!-- loc= takes a BARE stringtable key with no leading '#'. Keys live in this addon's own
                 stringtable.csv - EarPlugs must not reference a STR_BR_* or STR_MAP_* key. -->
            <input name="UAVigridEarPlugsToggle" loc="STR_EARPLUGS_KEYBIND_TOGGLE" />
        </actions>

        <sorting name="vigridearplugs" loc="STR_EARPLUGS_KEYBIND_CATEGORY">
            <input name="UAVigridEarPlugsToggle" />
        </sorting>
    </inputs>
    <preset>
        <input name="UAVigridEarPlugsToggle">
            <btn name="kJ" />
        </input>
    </preset>
</modded_inputs>
